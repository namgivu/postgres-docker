#!/usr/bin/env bash
    IMAGE_NAME=postgres:10
      STACK_ID=postgres_10
  EXPOSED_PORT=20410

     POSTGRES_USER=postgres
 POSTGRES_PASSWORD=postgres

CONTAINER_NAME=nn_postgres__${STACK_ID}
  NETWORK_NAME=${NETWORK_NAME:-docker_network__${STACK_ID}}
